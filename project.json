"type": "module",
  "scripts": {
    "dev": "vite dev",
    "cards": "node scripts/generate-manifest.mjs",
    "prebuild": "node scripts/generate-manifest.mjs",
    "prebuild:dev": "node scripts/generate-manifest.mjs",
    "build": "vite build",
    "build:dev": "vite build --mode development",
    "preview": "vite preview",
